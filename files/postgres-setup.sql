CREATE TABLE IF NOT EXISTS db_calander
(
  case_id       INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  search_date   VARCHAR(50),
  case_no       VARCHAR(50) UNIQUE,
  heading_status VARCHAR(255),
  judge1        VARCHAR(255),
  judge2        VARCHAR(255),
  judge3        VARCHAR(255),
  lcourt        VARCHAR(255),
  venue         VARCHAR(255),
  case_ref      VARCHAR(50),
  title1        VARCHAR(255),
  title2        VARCHAR(255),
  type          VARCHAR(255),
  lc_judge      VARCHAR(255),
  nature        VARCHAR(255),
  last_updated  VARCHAR(50),
  result        VARCHAR(255),
  status        VARCHAR(255),
  track_line1   VARCHAR(255),
  track_line2   VARCHAR(255),
  track_line3   VARCHAR(255),
  track_line4   VARCHAR(255),
  track_line5   VARCHAR(255),
  track_line6   VARCHAR(255),
  track_line7   VARCHAR(255),
  track_line8   VARCHAR(255)
);
